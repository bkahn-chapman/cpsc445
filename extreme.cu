#include <iostream>
#include <vector>
#include <fstream>
#include <stdio.h>
#include <stdlib.h>
#include <cmath>
#include <array>
#include <string>
using namespace std;

int main () {
    ifstream inFS;
    inFS.open("input.csv");
    system("head input.csv");
    vector<int> nums;
    string num;
    char c;
    while(infs >> noskpws >> c)
    {
        cout << c << endl;
    }
}